---
name: lesson-design
description: 课件设计规范（课件的唯一约束来源）：着眼点（弱约束：真实场景、术语先来历后定义、教材深度、练习一次）、贯穿线索、复用与骨架（硬约束）。讲解角色与总控加载。题目与 lab 不归它管。
---

# 课件设计规范

**本文件管课件内核与内容写法，不管版式**：分节、标题、例子与代码块的位置由讲解角色按内容与学生偏好定，`MEMORY.md` 的个人偏好优先。页头、顶栏、主题开关、脚本由渲染器统一产出。题目、lab、实验说明页也不归本文件管。

## 一、着眼点（弱约束：按实际情况调整，不必强加内容）

1. **参考**：课件的结构与内容可参考科目 `RESOURCES.md` 收集到的认可度高的教材；缺的来源报给总控
2. **术语**：讲解术语时可以先讲它的历史来由，再给定义；中英对照；
3. **深度**：讲透前提、推导、边界与反例，不做速成课；简洁不等于浅
4. **练习**：
   - `kind: 实操` → 引导式实操：任务与断言由 `practice-evaluator` 出、落在 `lab/`；内容文件里对应一个练习块（lab 内容归出题人，位置与引入由你定）
   - `kind: 概念` → 轻量练习：敲一条命令、改一行看变化、在输出里指出某个东西；不落 lab、不算交付物、分步且每步立刻能看到结果
   - 练习块与练习步骤的标题按内容写**中文阶段名**，**不要用 L1-L4 标步骤**（那是题目的层级词汇）

## 二、线索

**最终目标**（这段知识与 `MISSION.md` 的关系）
**配套项目**（从早期就逐课埋线，当实操素材）
**工具对照**（编程类：亲手实现与现成库结果对照）

## 三、复用与骨架（硬约束）

块名与用途见下表；语法以 `<root>/docs/课件内容格式.md` 为准（本文件不复述）。

| 要讲的东西 | 用哪个块 | 设计口径 |
|---|---|---|
| 一节 | `## 标题` | ≤14 字（进左侧目录），一节一个着眼点；句式服从内容，不套模板 |
| 分点、步骤、对照 | `-` 列表 / 管道表 | 对照关系（说法 ↔ 写法 ↔ 边界）用表，其余用列表；列表项不写长段落 |
| 代码与终端 | ` ```lang ` 围栏 | 讲的代码要能跑；语言标签写清楚 |
| 练习块 | `::: practice <层级> \| <标题>` | 按「练习」那条着眼点安排 |
| 题目位置 | `::: quiz <层级> 锚点：<锚点文本>` | 只留位、不写题：题由 `practice-evaluator` 出，锚点文本逐字给它 |
| 提示 / 警告 / 中性提示 | `::: tip` / `::: warn` / `::: note` | 用在真会绊倒学生的地方，不拿它凑版面 |
| 配图 | `::: figure <相对路径>` / 示意图 `::: svg` | **图要够丰富**（见下）；选哪条路线、怎么产、`alt:`/`caption:` 写法与尺寸见 `learning-coach` 的「配图」 |
| 资源与相关课件 | `::: resources` / `::: related` | 只放真会去用的；给不出链接的参考书写成不带链接的条目 |

**配图要够丰富**：图不是装饰——**"前后对比、参数影响、结构/流程关系、数据分布"这类内容都需要用图来解释**，不能只用文字带过；

**一节的组织**：一个 `##` 一节，先场景与来历、再定义、再例子与代码、最后练习或题目位置收尾；

| 硬约束 | 怎么做 |
|---|---|
| 起点 | 先读 `<subject_path>/assets/` 组件库（复用样式与术语） |
| 文件名 | `<序号>-<节点id>.md`，序号 = 本节点在 `curriculum.yaml` 的 `nodes:` 里排第几（4 位补零，如 `0003`）、节点 id 原样照抄——上一课的「下节课」指针按这条规则预写|
| 归属 | 只认文件名：这课是哪个节点、挂主页路线图哪个位置；页头的 eyebrow 是给学生看的可读文字（`0001 · 第一份能提交的代码`），写不写节点 id 都不影响归属 |
| 上/下节课指针 | 渲染器按 `curriculum.yaml` 的 `nodes:` 顺序算（第一课没有上一个、最后一课没有下一个） |
| 顶栏与主题开关 | 顶栏、主题开关、脚本引用由渲染器产出；内容文件里出现块级 HTML 标签会被渲染器拦下 |
| 交付形态 | 内容文件 + 出题人的题库，渲染出的页面自包含（相对路径引用共享层与科目组件）、浏览器直开可用、适合反复回看 |

## 四、行文禁忌

1. **避免使用「肯定-否定」句式，如「不是...而是」**；不要先假定学生会往错的方向想
2. **避免使用生硬的修辞**，如「这个定义是从麻烦里长出来的」；简单的概念甚至不需要修辞
3. **避免代入用户视角行文**（如「编译落到你手上，就是一个具体动作」），不要"为了方便理解"反而加重用户负担
4. 非重点的章节或课程无须写得太详细。重点不明确会显著提高阅读负担
5. **不得将未学过的知识点放入当前课件**，常见情况为「引入用户不知道的库」「把用户没学过的东西放代码里面用」「大量的使用未学过的术语」，请把读者当作一个理解能力稍强的16岁学生，切记**读者不是全知全能的AI**
6. **不要过度口语化**，应当严格遵守下文的**教材式行文风格**
7. **交付前需重读交付物**，检查是否存在上述行文禁忌

## 五、行文风格（非常重要）

请以经典教材的教学性说明文体（didactic exposition）和教科书语体（textbook register）写作：以说明为主、辅以必要论证，保持学术语域与术语一致，概念首次出现时先定义，再用元话语和衔接把定义、例证、对比、小结等修辞关系自然串起来，借助脚手架式推进让读者从具体到抽象、由已知到未知地理解，语气克制、客观、清晰，少用口语、情绪和营销化表达，读起来像一本严谨、清晰、可自学的经典教材；例如可以这样落笔：「设函数 f 在区间 I 上有定义。若对任意 $x_1,x_2 \in I，当 x_1<x_2$ 时都有 $f(x_1) < f(x_2)$，则称 f 在 I 上严格递增。」「直观上，这意味着函数值随自变量增大而增大；但需要注意，单调性总是相对于某个区间而言的。」「本节先给出定义，再通过两个例子说明其用法，最后讨论常见误区。」

